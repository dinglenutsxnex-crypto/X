package com.rootdroid.inspector.virtual

// Kept for backwards compatibility — all logic lives in ContainerEngine now.
@Deprecated("Use ContainerEngine directly", replaceWith = ReplaceWith("ContainerEngine"))
object VirtualEngine
